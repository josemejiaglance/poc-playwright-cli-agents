name: Cobrowse Video E2E

on:
  push:

concurrency:
  group: cobrowse-e2e-${{ github.ref }}
  cancel-in-progress: true

jobs:
  playwright-visitor:
    name: Playwright visitor smoke
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
      - run: npm ci
      - run: npm run test:pw:install
      - run: npm run test:pw:ci -- tests/playwright/visitor
      - uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: playwright-test-results
          path: |
            test-results/
            playwright-report/
          retention-days: 7

  e2e-video:
    name: Cobrowse with video
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browser
        run: npm run cli:install-browser

      - name: Run cobrowse video E2E
        env:
          GLANCE_AGENT_USER: ${{ secrets.GLANCE_AGENT_USER }}
          GLANCE_AGENT_PASSWORD: ${{ secrets.GLANCE_AGENT_PASSWORD }}
          PLAYWRIGHT_CLI_CONFIG: .playwright/cli.config.ci.json
          CI: "1"
        run: npm run test:ci:e2e:video

      - name: Upload artifacts on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-cli-artifacts
          path: |
            .playwright-cli/
            ci-artifacts/
          include-hidden-files: true
          retention-days: 7
          if-no-files-found: ignore
