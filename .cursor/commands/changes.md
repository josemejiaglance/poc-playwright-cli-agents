---
name: changes
description: Simple overview of git changes
---

Run `git diff --stat --no-color` (use --staged if user says "staged").

Then show the output cleanly.

If there are many changes, just show the first part and say "(truncated)".
Keep response short.
